# 科研链证 —— 技术应用赛道实现清单与工期评估

> 选题：**科研链证 —— 基于区块链的学术成果原创性与AIGC贡献存证平台**
> 目标：一人、AI 辅助、**9月18日 → 10月9日（22天）**，同时投「行业方案」+「技术应用」两个赛道
> 整理日期：2026-09-17

---

## 一、最重要的认知：你不需要搭一个论文平台

这是最容易走错的一步。**"论文平台"（投稿、送审、同行评议、排版、出版）是完全不同的产品，和你的选题没关系。**

### 你要做的是「存证 + 核验」层，不是「出版」层

| 分层 | 谁来提供 | 你要不要做 |
| --- | --- | --- |
| 论文写作 / 排版 | Word、LaTeX、Overleaf | ❌ 不做 |
| 投稿 / 同行评议 / 出版 | 期刊系统、arXiv、知网 | ❌ **不做（也不是你能做的）** |
| **原创性存证与核验** | **← 这是你的产品** | ✅ **只做这一层** |
| 查重算法 | JPlag / simhash / 现成库 | ⚠️ 不自己实现，调用即可 |

**一句话说明白你的作品边界：**
> "科研链证**不替代**任何写作或投稿平台。它是一个**独立第三方存证与核验服务**——作者在任何地方写完论文/代码，都可以拿到这里留一个不可篡改的'指纹'；任何评审方、期刊、竞赛组委会都能**独立验证**这份成果的原创性与 AI 使用情况，而不需要信任存证平台本身。"

**这个定位有一个巨大的好处**：因为你是"独立服务"，所以**不需要对接知网、arXiv、期刊系统**，也就没有做不完的集成工作。评审老师只会觉得你想得清楚。

---

## 二、技术应用赛道必须实现的清单

### ✅ 必须实现（MVP，缺一不可）

| # | 模块 | 做了什么 | 为什么不能砍 |
| --- | --- | --- | --- |
| **1** | **钱包连接与身份绑定** | MetaMask 连接 → 获得地址 → 绑定学号（学号只存哈希） | PPT 明确"必须有区块链要素"；没有钱包就没有"链"的入口 |
| **2** | **成果登记（存证）** | 选文件 → **浏览器本地算 SHA-256** → 填元信息 → 上链 → 返回 workId 与链上时间戳 | 核心功能，整个作品的地基 |
| **3** | **AIGC 贡献声明** | 声明使用了哪些 AI 工具、介入比例（0-100%）、用途说明 → 上链 | 这是你区别于所有"普通存证"作品的**唯一差异化卖点** |
| **4** | **核验（verify）** | 任何人上传文件 → 本地算哈希 → 查链 → 返回「存在 / 不存在」「作者」「登记时间」「AIGC声明」 | **演示效果的来源**：改一个字就核验失败 |
| **5** | **存证详情 / 时间轴** | 一个成果的完整履历：登记 → AIGC声明 → 版本追加 → 争议记录 | 可视化，PPT 要求"优秀的图形界面" |

### 🔶 建议实现（低成本、高加分）

| # | 模块 | 成本 | 加分点 |
| --- | --- | --- | --- |
| 6 | **版本追加** | 低（合约加个 map） | 体现"过程留痕"，不是一次性快照 |
| 7 | **存证证书导出** | 低（jsPDF 生成PDF） | 演示时"一键出证"很直观 |
| 8 | **争议登记** | 低 | 展示治理闭环 |
| 9 | **数据看板** | 中（ECharts） | 统计存证数、AIGC占比分布，界面好看 |

### ❌ 明确不做（列出来是为了防止你自己给自己加活）

| 不做 | 原因 |
| --- | --- |
| ❌ 用户注册/登录/找回密码系统 | 钱包地址就是身份，做了纯属浪费时间 |
| ❌ 真正的论文查重算法/NLP模型 | 这是另一个研究课题；用现成库算个相似度即可 |
| ❌ PDF 内容解析与语义比对 | 同上，会上瘾且做不完 |
| ❌ IPFS / 分布式文件存储的完整对接 | 可选加分项，但**原文不上链是对的**，别为了时髦加复杂度 |
| ❌ 对接 arXiv / 知网 / 期刊系统 | 无 API、无授权、无必要 |
| ❌ 后台管理端 / 管理员审核流程 | 联盟链场景才需要，demo 用不上 |
| ❌ 移动端 App / 小程序 | 网页端就够了 |
| ❌ 多链支持 / 跨链 | 明确超出范围 |
| ❌ 真实可信时间戳（TSA）对接 | 链上 block.timestamp 已足够演示 |

### 📊 结论：**5个核心模块 + 4个加分模块，纯软件、无硬件依赖、可单页应用承载。**

---

## 三、完整技术方案

### 3.1 角色与流程

**三个角色**（演示时你一人分饰，但要做得像三个独立方）：

| 角色 | 干什么 |
| --- | --- |
| **作者（学生）** | 连接钱包 → 上传成果算指纹 → 声明AIGC → 获得存证 |
| **核验方（期刊/竞赛/教务）** | 拿到一份文件 → 上传 → 得到"是否与链上一致"的结论 |
| **仲裁方（教务/组委会）** | 处理争议、查看履历 |

**核心流程（这就是你要录的演示视频脚本）：**
```
作者：连接钱包 → 选择论文.docx → 浏览器本地计算 SHA-256（文件不上传服务器！）
     → 填写标题/作者/AIGC声明(使用了ChatGPT，辅助润色，占比约20%)
     → 提交上链 → 返回 workId + 链上时间戳 + 存证证书

核验方：拿到同一份文件 → 拖入核验页 → 本地算哈希 → 查链
     → ✅ "核验通过：该文件与 2026-09-25 14:32 登记的成果完全一致，作者 0x1a2b..."

关键一幕：手动在文档里改一个标点 → 再次核验
     → ❌ "核验失败：该文件与链上任何登记记录均不匹配"（相似度 99.8%，但指纹不同）
```

> 💡 **"文件不上传服务器、只在浏览器本地算哈希"是一个极强的技术亮点**，要写进文档并口头强调——它同时解决了"隐私"和"信任"两个问题（存证平台自己都拿不到你的原文，自然无法作恶）。这一点会让评委眼前一亮。

### 3.2 智能合约（Solidity，约 180-220 行）

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract AcademicProof {
    enum WorkType { Paper, Code, Dataset, Other }

    struct Work {
        bytes32  contentHash;   // 文件 SHA-256 指纹
        address  author;        // 登记人钱包
        uint64   registeredAt;  // 首次登记时间（链上）
        uint64   updatedAt;     // 最后版本时间
        uint8    aigcRatio;     // AIGC 介入比例 0-100
        WorkType workType;
        string   title;
        string   metaURI;       // 元信息（可指向 IPFS 或链下）
        bool     disputed;      // 是否处于争议中
        uint32   versionCount;
    }

    address public admin;
    uint256 public workCount;
    mapping(uint256 => Work)                        private works;
    mapping(bytes32 => uint256)                     public  hashToWorkId;   // 指纹 → 成果ID
    mapping(uint256 => mapping(uint32 => bytes32))  public  versionHash;    // 版本 → 指纹
    mapping(uint256 => string[])                    public  aigcTools;      // AIGC 工具清单

    event WorkRegistered(uint256 indexed workId, bytes32 contentHash, address indexed author, uint64 timestamp);
    event VersionAdded(uint256 indexed workId, uint32 version, bytes32 contentHash);
    event AIGCDeclared(uint256 indexed workId, uint8 ratio, string tools);
    event DisputeRaised(uint256 indexed workId, address indexed raiser, string reason);

    error AlreadyRegistered();
    error NotAuthor();
    error InvalidRatio();
    error NotFound();

    constructor() { admin = msg.sender; }

    /// @notice 登记一份新成果
    function registerWork(
        bytes32  _contentHash,
        string   calldata _title,
        WorkType _type,
        uint8    _aigcRatio,
        string   calldata _metaURI
    ) external returns (uint256 workId) {
        if (hashToWorkId[_contentHash] != 0) revert AlreadyRegistered();
        if (_aigcRatio > 100) revert InvalidRatio();

        workId = ++workCount;
        works[workId] = Work({
            contentHash:  _contentHash,
            author:       msg.sender,
            registeredAt: uint64(block.timestamp),
            updatedAt:    uint64(block.timestamp),
            aigcRatio:    _aigcRatio,
            workType:     _type,
            title:        _title,
            metaURI:      _metaURI,
            disputed:     false,
            versionCount: 1
        });
        hashToWorkId[_contentHash] = workId;
        versionHash[workId][1] = _contentHash;

        emit WorkRegistered(workId, _contentHash, msg.sender, uint64(block.timestamp));
    }

    /// @notice 追加新版本（过程留痕）
    function addVersion(uint256 _workId, bytes32 _contentHash, uint8 _aigcRatio) external {
        Work storage w = works[_workId];
        if (w.author == address(0)) revert NotFound();
        if (w.author != msg.sender) revert NotAuthor();
        if (_aigcRatio > 100) revert InvalidRatio();

        w.versionCount += 1;
        w.contentHash = _contentHash;
        w.aigcRatio   = _aigcRatio;
        w.updatedAt   = uint64(block.timestamp);
        versionHash[_workId][w.versionCount] = _contentHash;
        hashToWorkId[_contentHash] = _workId;

        emit VersionAdded(_workId, w.versionCount, _contentHash);
    }

    /// @notice 声明 AIGC 使用情况
    function declareAIGC(uint256 _workId, uint8 _ratio, string calldata _tools) external {
        Work storage w = works[_workId];
        if (w.author == address(0)) revert NotFound();
        if (w.author != msg.sender) revert NotAuthor();
        if (_ratio > 100) revert InvalidRatio();

        w.aigcRatio = _ratio;
        w.updatedAt = uint64(block.timestamp);
        aigcTools[_workId].push(_tools);

        emit AIGCDeclared(_workId, _ratio, _tools);
    }

    /// @notice 核验：任意人可调用，不消耗状态
    function verifyWork(bytes32 _contentHash)
        external view
        returns (bool exists, uint256 workId, address author, uint64 registeredAt, uint8 aigcRatio, string memory title)
    {
        workId = hashToWorkId[_contentHash];
        if (workId == 0) return (false, 0, address(0), 0, 0, "");
        Work storage w = works[workId];
        return (true, workId, w.author, w.registeredAt, w.aigcRatio, w.title);
    }

    /// @notice 发起争议
    function raiseDispute(uint256 _workId, string calldata _reason) external {
        Work storage w = works[_workId];
        if (w.author == address(0)) revert NotFound();
        w.disputed = true;
        emit DisputeRaised(_workId, msg.sender, _reason);
    }

    function getWork(uint256 _workId) external view returns (Work memory) {
        return works[_workId];
    }
}
```

**合约设计要点（写进文档会加分）：**
- 用 **custom errors** 而不是 `require` 字符串 → 省 gas，体现工程规范
- `hashToWorkId` 实现 **O(1) 核验**
- `verifyWork` 是 `view`，**核验零成本**
- 版本通过 `versionHash[workId][version]` 保留全部历史指纹
- 事件完整，便于链下索引

### 3.3 前端（Vue3 单页应用）

**技术栈**：Vue3 + Vite + ethers.js v6 + Element Plus + ECharts + jsPDF

**页面（5个，一个SPA路由搞定）**

| 路由 | 页面 | 关键交互 |
| --- | --- | --- |
| `/` | 首页 | 一句话价值主张 + 连接钱包按钮 |
| `/register` | 存证登记 | 拖拽上传 → **实时显示计算出的哈希** → 填AIGC声明 → 提交 → 成功动画 |
| `/verify` | **核验（重点页）** | 拖入文件 → 本地算哈希 → 大字结论：✅一致 / ❌不一致 |
| `/work/:id` | 存证详情 | 时间轴（登记→版本→AIGC声明→争议）+ 元信息 + 导出证书 |
| `/dashboard` | 数据看板 | 存证总数、AIGC占比分布饼图、按类型柱状图 |

**核心代码：浏览器本地算哈希（这是你的技术亮点所在）**
```js
// utils/hash.js —— 文件永远不离开用户的浏览器
export async function sha256File(file) {
  const buf = await file.arrayBuffer();
  const digest = await crypto.subtle.digest('SHA-256', buf);
  return '0x' + [...new Uint8Array(digest)]
    .map(b => b.toString(16).padStart(2, '0')).join('');
}
```

**核验页的关键逻辑**
```js
const hash = await sha256File(file);
const [exists, workId, author, registeredAt, aigcRatio, title] =
  await contract.verifyWork(hash);
// exists === true  → 展示"核验通过"卡片
// exists === false → 展示"核验失败"，并提示"文件可能被修改过"
```

### 3.4 数据存储原则

| 数据 | 存哪 | 为什么 |
| --- | --- | --- |
| 文件**指纹**（SHA-256） | **链上** | 这是核验的唯一依据 |
| 登记人、时间戳、AIGC比例、类型 | **链上** | 需要不可篡改 |
| **原文文件** | **用户本地，不上传** | 隐私 + 降低复杂度 + 技术亮点 |
| 标题、摘要等文本元信息 | 链上（短字符串）或链下 | demo 直接上链，够用 |
| 可选：大元数据 | IPFS（**选做**） | 只有时间充裕才做 |

---

## 四、链的选择：两个方案，建议选 A

| | **A. FISCO BCOS（联盟链，推荐）** | B. Sepolia 测试网（公链） |
| --- | --- | --- |
| 与大赛契合度 | ⭐⭐⭐⭐⭐ PPT 明确点名"联盟链环境配置" | ⭐⭐⭐ |
| 费用 | **完全免费**，本地/云服务器自建 | 需领测试币（免费但要等） |
| 判分叙事 | "教学可信联盟链：学校、期刊、竞赛组委会共同作为共识节点" —— **完全对应你的场景** | "部署在公链测试网" |
| 工具链 | `build_chain.sh` 一键建链 + WeBASE + SmartDev 脚手架 | Hardhat（生态最成熟） |
| 上手成本 | 中（约1-2天环境） | **低（半天）** |
| 演示可访问性 | 本地跑，录屏演示 | ✅ **可部署到 Vercel，评委点链接就能自己试** |

### ✅ 推荐做法：**A + B 都做**
- **主链用 FISCO BCOS** 建一条 2-4 节点的联盟链 → 写进架构图和文档 → **这是拿分点**
- **同时把合约部署一份到 Sepolia** → 前端连 Sepolia → 部署到 Vercel，给评委一个**可点击的在线 Demo 链接**

> 这样既踩中 PPT 的"联盟链"口味，又有"在线可试"的杀手锏。成本只多半天。

参考文档：
- [FISCO BCOS build_chain.sh 建链工具](https://fisco-bcos-documentation.readthedocs.io/zh-cn/stable/docs/manual/build_chain.html)
- [SmartDev 脚手架快速开始](https://smartdev-doc.readthedocs.io/zh-cn/stable_contract_v1.0.0/docs/WeBankBlockchain-SmartDev-Scaffold/quick_start.html)

---

## 五、工期评估：**净开发约 9-11 天，22天窗口足够**

假设你：**有 C/C++、Linux 基础（大三 CS 已具备）；区块链零基础或入门；每天投入 5-6 小时；AI 全程辅助**

| 阶段 | 天数 | 日历 | 产出 | AI 承担比例 | 卡点提示 |
| --- | --- | --- | --- | --- | --- |
| **0. 环境搭建** | 1.5 | 9/18-19 | Node.js、Hardhat、MetaMask、FISCO BCOS 建链、Remix | 30% | FISCO 建链可能踩坑，**先用 Sepolia 跑通再回头搞联盟链**，避免卡死 |
| **1. 合约开发** | 2 | 9/20-21 | 上面那份合约写完 + 编译通过 + 部署 | **70%** | 合约 AI 基本能一次成型，你负责编译和调错 |
| **2. 合约测试** | 1.5 | 9/22-23 | Hardhat 单元测试（8-12个用例）+ 覆盖率 + gas 报告 | **60%** | 这是"技术规范"10分和答辩的硬材料 |
| **3. 前端骨架** | 2 | 9/24-25 | Vue3 项目 + 钱包连接 + 路由 + UI 框架搭好 | **70%** | ethers.js v6 的 API 和 v5 差别大，让 AI 统一用 v6 |
| **4. 核心页面** | 3 | 9/26-28 | 存证登记页 + **核验页** + 详情时间轴 | **60%** | 工作量最大的一块，但都是常规 CRUD |
| **5. 加分模块** | 2 | 9/29-30 | 证书导出 + 数据看板 + 争议功能 | **70%** | 时间不够就砍，**优先保证核验页** |
| **6. 系统测试** | 1.5 | 10/1-2 | 功能/安全/性能测试，出测试报告和数据 | 40% | 见下方"测试要交什么" |
| **7. 演示视频** | 1 | 10/3 | 3-5分钟录屏（写脚本、彩排、录制） | 30% | **必须提前写脚本**，即兴录会废掉半天 |
| **8. 提案书文档** | 3 | 10/4-6 | 概要设计 + 详细设计 + 测试与分析（双赛道共用） | **65%** | 这是行业方案赛道的主体，不能省 |
| **9. 打包提交** | 1 | 10/7-8 | 代码仓库整理 + PPT + 双赛道提交 | 40% | **10/8 提交** |

**合计净工时：约 9-11 个工作日**（不含缓冲），**日历占用 21 天**。

**✅ 结论：22天窗口内可以完成，而且留了约 1-2 天缓冲。**

### ⚠️ 但风险点必须说清楚

| 风险 | 概率 | 应对 |
| --- | --- | --- |
| FISCO BCOS 建链踩坑，卡住 2-3 天 | 中高 | **先 Sepolia 跑通主流程**，FISCO 作为"加分项"后补 |
| 前端 UI 调试超时（最常见） | 高 | **用 Element Plus 组件库，不自己写样式**；砍掉动画和花活 |
| 文档写得比预期久 | 中 | 从第1天就开始写文档，**不要留到最后** |
| 22天里还有课/实验/其他事 | 高 | 见下方"降级方案" |

### 📉 降级方案（务必现在就定好触发条件）

| 时间节点 | 检查点 | 如果没达标 |
| --- | --- | --- |
| **9月30日** | 合约已部署 + 存证/核验两个页面能跑通 | 若未达标 → **砍掉数据看板、争议、版本追加、FISCO 联盟链**，只保 4 个核心页面 |
| **10月3日** | 演示视频已录完 | 若未达标 → **放弃技术应用赛道，只投行业方案赛道**（不需要代码，照样参赛） |
| **10月5日** | 提案书主体完成 | 若未达标 → 技术应用赛道交"提案书 + 部分代码 + 演示视频"，**如实说明完成度**（一票否决里有"关键信息缺失"，但没禁止"部分实现"） |

> **保底逻辑很清晰**：行业方案赛道**根本不需要代码**。所以即使代码全废，你只要在 10月9日前交出一份提案书，这个比赛就没白参加。**这是你双赛道策略的最大安全垫。**

---

## 六、测试要交什么（PPT 明确列了测试类型）

PPT 里列出的测试类型：功能测试、智能合约测试、性能测试、安全测试、兼容性测试、可用性测试、压力测试。

**不要全做，挑4项做出数据即可：**

| 测试类型 | 怎么做 | 交付什么 |
| --- | --- | --- |
| **① 智能合约测试** | Hardhat + Chai，写 8-12 个用例：正常登记 / 重复哈希拒绝 / 非作者改版本被拒 / AIGC比例>100被拒 / 核验不存在返回false / 版本追加 / 事件触发正确 | **测试通过截图 + 覆盖率报告** |
| **② 性能测试** | 1) `hardhat-gas-reporter` 出每个函数的 gas 消耗表 2) 前端测大文件哈希耗时（10MB/50MB/100MB） | **gas 消耗表 + 哈希耗时表**（这就是"技术指标实验数据"，PPT 要求"创新作品需体现技术指标的实验过程和明确的实验结果"） |
| **③ 安全测试** | 人工列举：重入（本合约无转账，安全）/ 整数溢出（0.8+ 内置检查）/ 权限控制（NotAuthor 已覆盖）/ 前端 XSS / 私钥管理说明 | **安全测试清单 + 结论** |
| **④ 功能测试** | 按演示流程走一遍，记录每一步的输入输出 | **测试用例表（10条左右）** |

**成本**：约 1.5 天。**收益很高**——"技术规范与文档"10分 + 答辩时的实验数据支撑。

**推荐工具**：
- [Hardhat 覆盖率报告](https://hardhat.org/docs/guides/testing/code-coverage)
- `hardhat-gas-reporter` 插件
- Slither（可选，做一次静态分析，截图当安全性佐证）

---

## 七、现在（9月18日）就该做的三件事

1. **先跑通 Sepolia 路线**：装 MetaMask → 领测试币 → Remix 部署那份合约 → 能调用 `registerWork` 和 `verifyWork`。
   **这一步半天，是整个项目的"最小可行性验证"。跑通了，后面都是体力活。**

2. **把演示脚本写出来**（不用写代码）：
   > 上传论文 → 链上存证 → 改一个字 → 核验失败 → 展示存证证书

   这段 3 分钟的脚本是整个作品的灵魂，**先有它，再谈技术**。

3. **开一个 Git 仓库，从今天起每天 commit**。
   PPT 明确要求"源代码文件夹：**证明作品完成时间、过程**"——**commit 记录就是你的原创性证据**。

---

## 附：一句话总结

> **你不用搭论文平台。你要做的是一个 5 页面的单页应用 + 一份 200 行的智能合约 + 一套测试报告。**
> **净开发 9-11 天，22 天窗口足够，且有"只投行业方案赛道"作为安全垫。**
> **成败的关键不是技术难度，而是第1天就先把演示脚本定下来，并且不等文档——从第一天就开始写。**
