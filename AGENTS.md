# AGENTS.md — 全国大学生数智链应用大赛 参赛项目

> 本文件是本文件夹的**项目级协作说明**，供新会话中的 AI 助手（及本人）快速接续工作。
> 最后更新：**2026-09-23**

---

## 1. 一句话状态

**已定稿选题「科研链证」，目标双投「区块链·行业方案赛道」+「区块链·技术应用赛道」。距作品提交截止（2026-10-09）仅剩 16 天。**

**当前进度（2026-09-24 更新）**：**阶段0、阶段1、阶段2 已全部完成**——Hardhat 工程就绪、`AcademicProof` 合约完整实现（约 250 行，含 6 处安全加固）、本地链自检 6 组 27 项全通过、**单元测试 31 用例全通过且覆盖率 100%**、**合约已真实部署到 Sepolia 并跑通全流程**。
**Sepolia 部署记录（最终版，2026-09-24 由用户本人执行）**：合约地址 **`0x0Caf124D677DF3250a6cc37772bcc9E0a1de125E`**，部署交易 `0xb56b8a9f…b071ab`，区块 **11770049**，gas 1,207,892，部署账户 `0x639BeFd5A6a19EE19FE7743CdB200c66F2877C08`（约 0.036 ETH）。权威记录见 `blockchain/deployments/sepolia.json`。
> 注：AI 诊断期间曾用同一账户部署过两个临时合约（`0x6b3c4e47…`、`0xBC71FbEB…`）以及若干 check-env 临时合约，**均非最终产物，前端一律使用上面的地址**。
**Git 远程仓库**：https://github.com/qiufenyizhi/academic-proof-chain.git （队长姓名：任伊宁 renyining）
**阶段0 剩余事项（仅剩用户本人操作）**：**Etherscan 源码验证**（绿色对勾，答辩硬证据；截至 09-24 尚未完成）。
**⚠️ 重要经验（已修复并归档）**：自定义 error 在本地链与真实网络的报错形状不同——本地 message 含错误名，真实 RPC 只给 `execution reverted`，错误名只在 `e.data` 选择器里。判定必须基于选择器 + 用 `staticCall`。详见 `docs/环境搭建手册.md`「已踩坑并修复」小节。
**下一步**：阶段3 前端骨架（Vue3 + Vite + ethers v6 + Element Plus），AI 可独立完成。

---

## 2. 硬性时间约束（最高优先级）

| 日期 | 事件 | 说明 |
| --- | --- | --- |
| **2026-10-09** | **作品提交截止** | 逾期不予受理，**内部死线定为 10-08** |
| 2026-10-16 ~ 10-30 | 各赛区初赛评审 | 择优推荐至国赛决赛 |
| 2026-11（约） | 全国决赛、现场答辩 | 具体时间待组委会通知 |

### 倒计时节点（务必按此排期）

| 检查日 | 达标标准 | 未达标时的降级动作 |
| --- | --- | --- |
| **09-25** | 合约能在 Sepolia 部署，`registerWork` / `verifyWork` 调用成功 | 立即改用最简合约，砍掉版本/争议功能 |
| **09-30** | 存证页 + 核验页可跑通完整流程 | 砍掉数据看板、争议、版本追加、FISCO 联盟链，只保 4 个核心页面 |
| **10-03** | 演示视频已录制完成 | **放弃技术应用赛道，只投行业方案赛道**（不需代码，照样参赛） |
| **10-05** | 提案书主体完成 | 技术应用赛道交「提案书 + 部分代码 + 演示视频」，如实说明完成度 |
| **10-08** | 双赛道提交完毕 | — |

> **安全垫**：行业方案赛道**不强制要求源代码**。即使代码全废，只要 10-09 前交出一份提案书，参赛即成立。**任何时候都不要因为代码没做完而放弃提交。**

### ⚠️ 提交命名规范（强制，2026-09-24 用户提供官方截图）

**根目录必须命名为 `学校全称-队长姓名-作品名称`（半角连字符），子文件夹必须为 `01_作品文件`、`02_作品展示`、`03_设计文档`、`04_作品信息`、`05_承诺书`、`06_源文件`；PDF 建议 PDF/A-1b。**
**完整规范与本项目材料对应关系见 `docs/作品提交命名规范.md`。**
**关键提醒**：① 不要现在重命名工作目录（会打断路径与 Git 配置），**10-07 打包时新建正式提交目录再复制** ② `队长姓名` 需用户本人提供 ③ **`05_承诺书` 需下载签字，建议 10-05 前完成**。

---

## 3. 参赛者情况（用于校准建议）

| 项 | 内容 |
| --- | --- |
| 学校 / 年级 | 东华大学 计算机科学与技术，**大三（2027届）** |
| 队伍规模 | **1 人（独立参赛）**，可 1-5 人，单人报名无劣势 |
| 推免加分 | **已拿满 0.2 上限**（数学建模美赛 M 奖），**本比赛不为加分，定位是「以赛代练」** |
| 技术背景 | C/C++、Linux 基础扎实；**区块链零基础或入门**；无 Go/Java 后端经验假定 |
| 每日可投入 | 约 5-6 小时（需兼顾课程） |
| 核心诉求 | 用 AI 辅助在 16 天内产出可交付作品；**工程量必须可控，拒绝硬件依赖** |

### 协作偏好（重要）
- 用户希望**直接可执行的产出**：代码、文档大纲、检查清单，而非泛泛建议
- 用户会明确说「保底 / 加投」的分层目标，**给建议时要保留降级路径**
- **避免夸大**：不确定的信息要标注不确定，不要让用户基于猜测做决策
- 中文交流

---

## 4. 本文件夹内容地图

### 原始资料（用户提供，只读参考）
| 文件 | 内容 |
| --- | --- |
| `区块链-行业应用赛道解读.pptx` | **技术应用赛道**官方解读。31页，含评分标准、优秀作品案例（警用无人机存证平台四层架构图、鲜享链DAPP业务流程图、智研汇平台）、答辩PPT章节结构、优秀作品6个必要条件 |
| `区块链-行业方案赛道解读.pptx` | **行业方案赛道**官方解读。19页，含选题案例（算法合规链、声声不息等）、设计文档四文件夹结构、测试类型清单、**第18页关键句："不硬性要求解决方案的原代码、Web端或移动端平台"** |
| `作品命名要求.png` | **官方命名规范截图** → 已录入为 `docs/作品提交命名规范.md` |

### 已于 2026-09-24 删除（不在工作区，仍存于 Git 历史 commit `7b41501`）
| 文件 | 原内容 |
| --- | --- |
| ~~`专项赛-AI动漫游戏赛道解读.pdf`~~ | AI+动漫游戏专项赛官方解读（该赛道已放弃） |
| ~~`新建 DOCX 文档.docx`~~ | 仅含两条微信推文链接，无实质内容 |
| ~~`赛道对比与选择建议.md`~~ | 10 个赛道横向对比与选择推理（**提案书「选题定位」素材来源，已删，需时取回**） |
| ~~`区块链-5个选题方案.md`~~ | 5 个候选选题完整方案（**同上**） |

### 本项目产出的分析文档
| 文件 | 内容 |
| --- | --- |
| `科研链证-技术应用赛道实现清单.md` | **本项目的核心技术文档**。含实现边界、Solidity 合约全文、前端设计、链选型、工期拆解、测试交付物 |
| `docs/环境搭建手册.md` | **阶段0 操作手册**。含分工说明、MetaMask 与水龙头步骤、合约 6 处加固说明、检查清单、故障排查表、**真实网络自定义 error 踩坑记录** |
| `docs/演示脚本-逐秒版.md` | **演示视频逐秒分镜 + 全文台词**（3分00秒）。含录制前物资清单、反推出的 10 条前端需求（D1-D10）、翻车预防 |
| `docs/测试报告-智能合约.md` | **阶段2 交付物（已提前完成）**。31 用例全通过 / **覆盖率 100%** / gas 消耗表 / 安全测试清单 / 复现步骤 |
| `docs/作品提交命名规范.md` | ⚠️ **提交强制要求**。根目录 `学校全称-队长姓名-作品名称`、六个 `01_`~`06_` 子文件夹、PDF/A-1b、`06_源文件` 二级分类 |

### 过程证据材料（**勿删，对应 PPT 的"证明作品完成时间、过程"**）
| 文件 | 内容 |
| --- | --- |
| `跑通本地最小验证/npx hardhat test（31 passing）.png` | 31 个用例全通过，9 组全绿 |
| `跑通本地最小验证/npx hardhat coverage（100%）.png` + `(2).png` | 覆盖率报告（语句/分支/函数/行 100%）与控制台输出 |
| `跑通本地最小验证/获得0.05SepoliaETH.png` | Google Cloud Web3 水龙头领币成功，MetaMask 显示 `0.0500 SepoliaETH` |
| `跑通本地最小验证/npx hardhat run scriptscheck-env.js --network sepolia.png` + `(2).png` | **真实 Sepolia 自检 27 项全通过**（修复后） |
| `跑通本地最小验证/npx hardhat run scriptsdeploy.js --network sepolia.png` | 部署输出：合约地址、交易哈希、区块、gas |
| `跑通本地最小验证/Etherscan交易.png` + `(2).png` | Etherscan 账户页：合约创建交易与 `Raise Dispute` 等调用记录 |
| `屏幕截图 2026-09-24 131742.png` | MetaMask 网络 `: Sepolia`、账户 `0x639Be...77C08` |

> ⚠️ **2026-09-24 用户已确认永久删除**：`区块链-5个选题方案.md`、`赛道对比与选择建议.md`、`专项赛-AI动漫游戏赛道解读.pdf`、`新建 DOCX 文档.docx`。
> 前三者仍存于 Git 历史（commit `7b41501`），需要时可用 `git show 7b41501:<文件名>` 取回。
> **影响提示**：选题方案的推理过程与赛道对比原是提案书「选题定位」章节的素材来源，删除后该章节需重新组织内容。

### 关键结论速查（避免重复调研）

**大赛基本规则**
- 官网：https://www.dabc.org.cn ｜ 报名系统：http://dasai.dabc.org.cn/
- 组委会秘书处：**周老师 13611113305**
- **不收报名费、参赛费**
- 每队 **1-5 名学生 + 1-2 名指导教师**，可跨校组队，**每校作品数量不限**
- 三大类 9 个常规赛道 + 1 个专项赛；**100分制 + 一票否决制**，分类评审、单独设奖

**一票否决红线（绝不能碰）**
1. 选题存在意识形态问题，违反法律法规、公序良俗
2. 抄袭、侵权、盗用数据与代码，使用非授权素材
3. **作品非本届大赛规定周期内原创完成**
4. **文档、演示、系统存在造假或关键信息缺失**
5. 含其他赛事标识、违规水印、商业广告

**两个赛道的交付物差异**

| | 行业方案赛道 | 技术应用赛道 |
| --- | --- | --- |
| 交付物 | 创意提案书 + 承诺书 | 设计文档 + 演示视频 + **源代码** + 承诺书 |
| 是否强制代码 | **不强制**（PPT 第18页明确） | **必须** |
| 评分构成 | 选题定位15 / **设计完成与呈现50** / 文档10 / 展示15 / 价值前景10 | 选题定位10 / **设计完成与呈现50** / 文档10 / 展示答辩15 / 价值前景10 |

**优秀作品公式（从 PPT 范例反推）**
- 命名：`"[具象名前缀]链 —— 基于区块链的 XX 存证/治理平台"`
- 区块链必要性必须归属：**存证 / 溯源 / 确权 / 自动执行** 四类之一
- 架构必须画成**四层**：系统架构层 / 用户层 / 网络·共识层 / 应用·服务层
- 文档需要：**概要设计 + 详细设计 + 测试与分析** 三大块
- PPT 明确点名可融合方向：AIGC、大模型、艺术创作、数字经济、工业互联网等
- 加分项：「部署云应用」「第三方测试报告或应用证明」

---

## 5. 已定稿的产品定义（不要推翻）

### 作品名称
**科研链证 —— 基于区块链的学术成果原创性与AIGC贡献存证平台**

### 一句话定位（提案书开篇就用这句）
> 科研链证**不替代**任何写作或投稿平台。它是一个**独立第三方存证与核验服务**——作者在任何地方写完论文/代码，都能在这里留下一个不可篡改的「指纹」；任何评审方、期刊、竞赛组委会都能**独立验证**其原创性与 AI 使用情况，**而不需要信任存证平台本身**。

### 痛点（提案书第一章）
1. **查重结论不可独立验证** —— 现有查重系统只给一个百分比，结果由平台自证，第三方无法复现，报告可 PS
2. **AIGC 贡献无据可查** —— AI 写了多少、人工改了多少，完全没有记录手段（**这是核心差异化卖点**）
3. **时间戳争议** —— 代码作业 / 竞赛作品的「谁先谁后」缺乏可信时间证据
4. **成果确权脆弱** —— 中心化存档一旦丢失或篡改无从追溯

### 为什么必须上链（答辩必问题）
- 内容指纹 + 时间戳上链 → 任何人拿原文重算哈希即可验证，**不依赖平台自证**
- AIGC 贡献比例一经上链不可篡改 → 作者无法事后改口
- 智能合约自动出证 → 相似度超阈值自动触发仲裁流程

---

## 6. 实现边界（本节最重要，防止范围蔓延）

### ✅ 必须实现 —— 5 个核心模块

| # | 模块 | 关键点 |
| --- | --- | --- |
| 1 | **钱包连接与身份绑定** | MetaMask 连接 → 绑定学号（**学号只存哈希**） |
| 2 | **成果登记（存证）** | 选文件 → **浏览器本地算 SHA-256** → 填元信息 → 上链 → 返回 workId + 链上时间戳 |
| 3 | **AIGC 贡献声明** | 声明 AI 工具、介入比例（0-100）、用途 → 上链 |
| 4 | **核验 verify** | 任何人上传文件 → 本地算哈希 → 查链 → 返回「存在/不存在 + 作者 + 登记时间 + AIGC声明」 |
| 5 | **存证详情 / 时间轴** | 登记 → AIGC声明 → 版本追加 → 争议，可视化履历 |

### 🔶 加分模块（时间够再做，按此优先级）
1. 版本追加（追加新版本指纹，体现"过程留痕"）
2. 存证证书导出 PDF（jsPDF）
3. 争议登记（raiseDispute）
4. 数据看板（ECharts：存证总数、AIGC占比分布）

### ❌ 明确不做（列出以防止加活）
- ❌ 用户注册 / 登录 / 找回密码系统（钱包地址即身份）
- ❌ 真正的论文查重算法 / NLP 模型（另一个研究课题）
- ❌ PDF 内容解析与语义比对
- ❌ IPFS / 分布式存储完整对接（原文不上链是对的，别为时髦加复杂度）
- ❌ 对接 arXiv / 知网 / 期刊系统（无 API、无授权、无必要）
- ❌ 后台管理端 / 管理员审核流程
- ❌ 移动端 App / 小程序
- ❌ 多链支持 / 跨链
- ❌ 真实可信时间戳（TSA）对接（`block.timestamp` 足够演示）
- ❌ 任何硬件依赖（传感器、开发板、无人机）——单人 16 天做不出 demo

---

## 7. 技术方案（已定稿）

### 技术栈
| 层 | 选型 |
| --- | --- |
| 智能合约 | Solidity ^0.8.20（用 custom errors，不用 require 字符串） |
| 合约开发/测试 | Hardhat + Chai + `hardhat-gas-reporter` + `solidity-coverage` |
| 前端 | Vue3 + Vite + **ethers.js v6**（注意：v6 API 与 v5 差异大，务必统一用 v6）+ Element Plus + ECharts + jsPDF |
| 主链 | **FISCO BCOS 联盟链**（本地建 2-4 节点，对应 PPT 的"联盟链环境配置"） |
| 演示链 | **Sepolia 测试网**（前端连它，部署到 Vercel 给评委在线试用） |
| 部署 | Vercel / GitHub Pages（纯前端，`/` 无后端即可运行） |

> **双链策略**：FISCO BCOS 用于架构图与文档拿分；Sepolia 用于"在线可点 Demo"。
> **执行顺序建议**：**先用 Sepolia 跑通全流程**，FISCO 联盟链作为后补加分项，避免建链踩坑卡死进度。

### 关键技术亮点（答辩时必须口头强调）
**文件永远不上传服务器，只在浏览器本地计算哈希。**

```js
// utils/hash.js
export async function sha256File(file) {
  const buf = await file.arrayBuffer();
  const digest = await crypto.subtle.digest('SHA-256', buf);
  return '0x' + [...new Uint8Array(digest)]
    .map(b => b.toString(16).padStart(2, '0')).join('');
}
```

这一招同时解决**隐私**与**信任**两个问题——存证平台自己都拿不到原文，自然无法作恶。

### 数据存储原则
| 数据 | 存哪 |
| --- | --- |
| 文件指纹 SHA-256 | **链上**（核验的唯一依据） |
| 登记人、时间戳、AIGC比例、类型 | **链上** |
| **原文文件** | **用户本地，永不上传** |
| 标题等短文本元信息 | 链上（demo 直接上链即可） |
| 大元数据 | IPFS（**选做，时间充裕才做**） |

### 智能合约设计
完整代码见 `科研链证-技术应用赛道实现清单.md`（约 180-220 行）。核心结构：

```solidity
contract AcademicProof {
    enum WorkType { Paper, Code, Dataset, Other }
    struct Work {
        bytes32 contentHash; address author;
        uint64 registeredAt; uint64 updatedAt;
        uint8 aigcRatio; WorkType workType;
        string title; string metaURI;
        bool disputed; uint32 versionCount;
    }
    mapping(uint256 => Work) private works;
    mapping(bytes32 => uint256) public hashToWorkId;            // O(1) 核验
    mapping(uint256 => mapping(uint32 => bytes32)) public versionHash;

    function registerWork(bytes32, string, WorkType, uint8, string) external returns (uint256);
    function addVersion(uint256, bytes32, uint8) external;
    function declareAIGC(uint256, uint8, string) external;
    function verifyWork(bytes32) external view returns (bool, uint256, address, uint64, uint8, string);
    function raiseDispute(uint256, string) external;
    function getWork(uint256) external view returns (Work memory);
}
```
设计要点：custom errors 省 gas ／ `hashToWorkId` 实现 O(1) 核验 ／ `verifyWork` 为 `view` 核验零成本 ／ 事件完整便于链下索引。

### 前端页面（SPA，5 个路由）
| 路由 | 页面 | 关键交互 |
| --- | --- | --- |
| `/` | 首页 | 价值主张 + 连接钱包 |
| `/register` | 存证登记 | 拖拽上传 → **实时显示哈希** → 填 AIGC 声明 → 提交 → 成功动画 |
| `/verify` | **核验（重点页）** | 拖入文件 → 本地算哈希 → 大字结论 ✅一致 / ❌不一致 |
| `/work/:id` | 存证详情 | 时间轴 + 元信息 + 导出证书 |
| `/dashboard` | 数据看板 | 存证总数、AIGC 占比饼图、按类型柱状图 |

---

## 8. 演示脚本（作品的灵魂，先有它再谈技术）

**3 分钟演示视频，必须体现"篡改即失效"的戏剧性：**

```
1. 作者连接钱包
2. 上传论文.docx → 浏览器本地计算 SHA-256（强调文件未上传服务器）
3. 填写标题 + AIGC声明（使用了ChatGPT，辅助润色，占比约20%）→ 提交上链
4. 返回 workId + 链上时间戳 + 存证证书（导出PDF）

5. 切换角色为「核验方」→ 拿到同一份文件 → 拖入核验页
   → ✅ 核验通过：该文件与 2026-09-25 14:32 登记的成果完全一致，作者 0x1a2b...

6. 【关键一幕】手动在文档里改一个标点 → 再次核验
   → ❌ 核验失败：该文件与链上任何登记记录均不匹配
```

**"篡改即失效"是所有区块链作品里最有说服力的演示效果。**

---

## 9. 剩余 16 天排期（2026-09-23 → 10-08）

| 阶段 | 日期 | 天数 | 产出 | 卡点提示 |
| --- | --- | --- | --- | --- |
| **0. 环境 + 最小验证** | **09-23 ~ 09-24** | 2 | 装 Hardhat/MetaMask，领 Sepolia 测试币，Remix 部署合约，`registerWork`+`verifyWork` 调通 | **这是整个项目的最小可行性验证，跑通后全是体力活** |
| **1. 合约开发** | 09-25 ~ 09-26 | 2 | 合约写全 + 编译 + 部署脚本 | AI 基本能一次成型，你负责编译调错 |
| **2. 合约测试** | 09-27 | 1 | Hardhat 8-12 用例 + 覆盖率 + gas 报告 | 直接对应"技术规范"10分与答辩数据 |
| **3. 前端骨架** | 09-28 ~ 09-29 | 2 | Vue3 + 钱包连接 + 路由 + Element Plus | ethers.js **统一用 v6** |
| **4. 核心页面** | 09-30 ~ 10-01 | 2 | 存证页 + **核验页** + 详情时间轴 | 工作量最大块，时间不够先保核验页 |
| **5. 加分模块** | 10-02 | 1 | 证书导出 / 看板 / 争议 | 可整体砍掉 |
| **6. 系统测试** | 10-03 | 1 | 功能/安全/性能测试 + 数据 | 见下节 |
| **7. 演示视频** | 10-04 | 1 | 3分钟录屏 | **必须提前写脚本并彩排** |
| **8. 提案书文档** | 10-04 ~ 10-06 | 3 | 概要设计 + 详细设计 + 测试与分析 | **行业方案赛道的主体，不能省** |
| **9. 打包提交** | 10-07 ~ 10-08 | 2 | 代码仓库整理 + PPT + 双赛道提交 | 10-08 提交，不要等 10-09 |

**总计约 15-16 天，几乎没有缓冲。** 因此：**文档从第 1 天就要开始写，不要留到最后。**

---

## 10. 测试交付物（PPT 明确要求"技术指标实验数据"）

PPT 列出的测试类型：功能 / 智能合约 / 性能 / 安全 / 兼容性 / 可用性 / 压力。
**不要全做，挑这 4 项做出数据即可：**

| 测试类型 | 怎么做 | 交付物 |
| --- | --- | --- |
| **① 智能合约测试** | Hardhat+Chai，8-12 用例：正常登记 / 重复哈希拒绝 / 非作者改版本被拒 / AIGC比例>100被拒 / 核验不存在返回 false / 版本追加 / 事件触发正确 | 测试通过截图 + **覆盖率报告** ← ✅ **已完成：31 用例（超出原计划）、覆盖率 100%**，证据在 `docs/测试报告-智能合约.md` |
| **② 性能测试** | 1) `hardhat-gas-reporter` 出各函数 gas 表 2) 前端测 10MB/50MB/100MB 文件哈希耗时 | **gas 消耗表 + 哈希耗时表** ← gas 表 ✅ 已完成；哈希耗时表待阶段6 |
| **③ 安全测试** | 重入（本合约无转账，安全）/ 整数溢出（0.8+ 内置）/ 权限控制（NotAuthor 覆盖）/ 前端 XSS / 私钥管理说明 | 安全测试清单 + 结论 |
| **④ 功能测试** | 按演示流程走一遍，记录每步输入输出 | 测试用例表（约 10 条） |

可选加分：Slither 静态分析截图。

---

## 11. 答辩预案：评委必问的 5 个问题

提前把答案写进文档，答辩就不慌：

1. **"这功能用普通数据库不也能做？为什么非要区块链？"**
   → 核心是**多方互不信任、且需要第三方独立验证**。中心化数据库里记录方 = 验证方，自证无效。

2. **"链下数据怎么保证上链前没被改过？"**
   → 设计"提交即上链"流程 + 多签见证 + 版本追加；**坦白承认**首次上链前的真实性依赖源头可信——这是所有上链方案的共同边界，坦白反而加分。

3. **"性能能撑住吗？"**
   → 明确说明**只有哈希和关键状态上链，原文与计算在链下**，这是标准分层设计。

4. **"你一个人做的？工作量匹配吗？"**
   → 主动说明独立完成了哪些部分（架构/合约/前端/文档），展示 Git commit 记录。

5. **"和已有的 XX 产品有什么区别？"**
   → 准备**竞品对比表**放在文档里（对比知网查重、区块链存证平台、Turnitin 等）。这是"选题定位"的隐藏加分项。

---

## 12. 待办与未决事项


### 🟡 技术待办（按顺序执行）

- [x] **【阶段0 已完成】环境搭建 + 最小可行性验证**（09-23 ~ 09-24）
  - [x] Node.js（已有 v24.15.0）、Hardhat 工程、依赖安装（591 包，已解决 peer 依赖冲突）
  - [x] `AcademicProof` 合约完整实现 + 编译通过
  - [x] `scripts/check-env.js` 一键自检：**6 组 27 项全部通过 ✅**（本地链 + 真实 Sepolia 双环境均通过）
  - [x] MetaMask 安装 + 切到 Sepolia（用户已完成）
  - [x] 领取 Sepolia 测试币 0.05 ETH（用户已完成，Google Cloud Web3 水龙头）
  - [x] **部署到 Sepolia**：合约 **`0x0Caf124D677DF3250a6cc37772bcc9E0a1de125E`**，区块 11770049
  - [x] 建 GitHub 远程仓库并 push → https://github.com/qiufenyizhi/academic-proof-chain.git
  - [ ] **Etherscan 源码验证**（绿色对勾）← **唯一剩余项，用户本人做，答辩硬证据**
- [x] 合约完整实现 + 部署脚本（`blockchain/src/contracts/AcademicProof.sol` + `scripts/deploy.js`，**提前于排期完成**，阶段1 将压缩为"部署到两套环境"）
- [x] 合约单元测试 + 覆盖率 + gas 报告 ← **已提前完成**：`blockchain/test/AcademicProof.test.js`，**31 用例全通过、覆盖率 100%（语句/分支/函数/行）**、部署 1,207,892 gas、核验 0 gas。详见 `docs/测试报告-智能合约.md`
- [ ] Vue3 前端骨架 + 钱包连接
- [ ] 存证页 → 核验页 → 详情时间轴
- [ ] 加分模块（按优先级）
- [ ] 系统测试与数据整理
- [ ] 演示视频录制
- [ ] 提案书撰写（概要设计 + 详细设计 + 测试与分析）
- [ ] PPT + 代码仓库整理 + 双赛道提交

### 📌 立即可做的低成本高价值动作

- [x] **开 Git 仓库，从今天起每天 commit** ← Git 身份已配置、`.gitignore` 已就绪、首次提交已完成；**用户还需建远程仓库并 push**
  → PPT 明确要求"源代码文件夹：**证明作品完成时间、过程**"。**commit 记录就是原创性证据**，一票否决红线第 3 条（非本届周期内原创）靠它自证。
- [x] **把演示脚本细化成逐秒脚本** → 见 `docs/演示脚本-逐秒版.md`（含全文台词 + 反推出的 10 条前端需求 D1-D10）

---

## 13. 给接续会话的 AI 助手的工作提示

1. **先读本文件，再读 `科研链证-技术应用赛道实现清单.md`**（含完整 Solidity 合约代码，可直接复用）
2. **每次开工先确认今天日期**，并对照第 9 节排期检查是否滞后；若滞后，主动提出降级建议（第 2 节降级表）
3. **严格遵守第 6 节的"❌ 明确不做"清单**。用户容易在项目中途想加功能，**遇到这类请求要先提醒范围风险**
4. **产出优先给可直接用的东西**：完整代码文件、文档章节正文、检查清单——而不是"你可以考虑……"
5. **不要建议任何硬件方案**，也不要建议引入需要额外学习成本的重型框架
6. **行业方案赛道始终是保底**。任何技术方案的建议都要保证"即使代码没做完，提案书仍然完整可交"
7. 文档写作顺序建议：**从第 1 天就开始写提案书**，代码与文档并行推进（PPT 要求文档包含概要设计、详细设计、测试与分析三大块）
8. 涉及大赛规则的问题，**优先建议用户直接致电周老师确认**，不要把推测当结论

---

## 14. 外部参考链接

**大赛官方**
- 官网：https://www.dabc.org.cn
- 报名系统：http://dasai.dabc.org.cn/
- [重要通知：报名时间、作品提交要求、评审标准](https://www.dabc.org.cn/news/news00023.html)
- [正式启动通知](https://www.dabc.org.cn/news/news000022.html)
- 组委会秘书处：周老师 13611113305

**技术文档**
- [FISCO BCOS 建链工具 build_chain.sh](https://fisco-bcos-documentation.readthedocs.io/zh-cn/stable/docs/manual/build_chain.html)
- [SmartDev 脚手架快速开始](https://smartdev-doc.readthedocs.io/zh-cn/stable_contract_v1.0.0/docs/WeBankBlockchain-SmartDev-Scaffold/quick_start.html)
- [WeBASE 交易接口说明](https://webasedoc.readthedocs.io/zh-cn/v1.5.3/docs/WeBASE-Transaction/interface.html)
- [Hardhat 测试覆盖率指南](https://hardhat.org/docs/guides/testing/code-coverage)

**素材提取工具（本项目自建）**
- 父目录 `../_tools/Extract-PdfText.ps1` —— 通用的 CID 字体中文 PDF 文本提取脚本（曾成功解析东华大学推免办法 PDF）。若需重新读取 PPT/PDF 原文，可复用该脚本或直接以 zip 方式解压 PPTX 取 `ppt/media/` 与 `ppt/slides/`。
