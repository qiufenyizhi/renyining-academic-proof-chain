# 区块链赛道选题方案（5选1）

> 目标：**一份作品双投**——保底「区块链·行业方案赛道」，进展顺利则加投「区块链·技术应用赛道」
> 约束：一个人、**9月18日 → 10月9日（22天）**、AI 辅助、工程量可控
> 整理日期：2026-09-17

---

## 〇、先说清楚：两个赛道的交付物差异

| | 行业方案赛道 | 技术应用赛道 |
| --- | --- | --- |
| 官方要求交付 | 完整**创意提案书** + 承诺书 | 设计文档 + **演示视频** + **源代码** + 承诺书 |
| 是否强制代码 | **不强制**（赛道解读 PPT 明确："不硬性要求解决方案的原代码、Web端或移动端平台"） | **必须** |
| 评审重心 | 选题定位15 + **设计完成与呈现50** + 文档10 + 展示15 + 价值前景10 | 选题10 + **设计完成与呈现50** + 文档10 + 展示答辩15 + 价值前景10 |

**核心策略：选题必须"能用纯软件 demo 演示"** ——这样提案书写完直接就能升级成技术应用赛道作品。
**排除掉一切需要硬件（传感器、开发板、无人机）的选题**，否则 demo 做不出来。

### 📌 从两份 PPT 里提炼出的"获奖级作品公式"

参考 PPT 给出的优秀作品：
- 基于区块链的**警用无人机安全存证平台**
- 智绘链：AI驱动的 NFT 创作
- 智研汇——融合智能合约与大语言模型的数字科研工具交易平台
- 鲜享链 DAPP：基于区块链的生鲜 NFT 礼券交易平台
- 面向智能合约重入漏洞的检测方法
- 算法合规链——AI司法审查与责任追溯平台
- 声声不息——基于区块链+AIGC的原创音乐认证平台

**它们的共同结构：**

| 要素 | 做法 |
| --- | --- |
| **命名** | "[具象名字] + 破折号 + 基于区块链的 XX 平台/系统" |
| **区块链的必要性** | 必须是"**存证 / 溯源 / 确权 / 自动执行**"四类之一，且离开链就解决不了 |
| **跨界融合** | PPT 明确点名"与 AIGC、大模型、艺术创作、数字经济、工业互联网、低空经济、自动驾驶结合" |
| **架构** | **四层**：系统架构层 / 用户层 / 网络·共识层 / 应用·服务层（这是 PPT 里的范例画法） |
| **文档** | 需要**概要设计 + 详细设计 + 测试与分析**三大块 |
| **演示** | 优秀的图形界面 + 设计图 + 优秀的视频讲解 + 完整代码解释 |
| **加分** | 部署云应用（"锦上添花"） |

---

## 一、5个选题总览对比

| # | 选题 | 工程量 | 技术亮点 | 有真实素材吗 | 风险 |
| --- | --- | --- | --- | --- | --- |
| **1** | 链上学术诚信：论文/代码原创性与AIGC贡献存证 | ★★★ | 内容指纹 + AIGC占比存证 | ✅ 你自己就是用户 | 低 |
| **2** | 算法合规链：AI决策审查与责任追溯（PPT范例同款） | ★★★★ | 决策血缘 + 可解释性上链 | ⚠️ 需构造案例 | 中（易做空） |
| **3** | 电商退货/二手转卖数字凭证（鲜享链同款结构） | ★★★ | NFT生命周期状态机 | ⚠️ 需虚拟数据 | 中（易同质） |
| **4** | 实验室/校园资源链上预约与信用治理 | ★★★★ | 链上信用分 + 预言机 | ✅ 学院真实痛点 | 低 |
| **5** | 可验证履历：竞赛/实习凭证的选择性披露 | ★★★ | 零知识/选择性披露 | ✅ 你自己有履历 | 中（ZK易过度承诺） |

---

## 二、5个选题详细方案

---

### 🥇 选题1：科研链证 —— 基于区块链的学术成果原创性与AIGC贡献存证平台

**一句话定位**：把"谁在什么时候提交了什么内容、AI参与了多少"变成任何人可独立验证的链上事实。

#### 痛点分析（真实、具体、你能讲得清）
1. **查重结论不可独立验证**：现有查重系统（知网、Turnitin）给出一个百分比，但结果只有平台自己说了算，**第三方无法复现验证**，且报告可被PS。
2. **AIGC 贡献无据可查**：现在论文/大作业大量使用 AI，但"AI 写了多少、人工改了多少"完全没有记录手段。评审方无从判断。
3. **时间戳争议**：idea 优先权、代码作业抄袭的"谁先谁后"缺乏可信时间证据。
4. **成果确权脆弱**：毕业设计、竞赛作品的原创性，学校/主办方存档是中心化的，一旦系统丢失或篡改无从追溯。

#### 为什么必须用区块链（评委最爱问的第一个问题）
- **内容指纹 + 时间戳上链** → 任何人拿原文重新计算哈希即可验证，**不依赖平台自证**
- **AIGC 贡献比一经上链不可篡改** → 作者无法事后改口
- **智能合约可自动出证** → 相似度超过阈值自动触发告警/仲裁流程，无需人工介入

#### 系统架构（四层，照 PPT 范式画）
```
系统架构层：FISCO-BCOS / 以太坊测试网 · SmartDev 脚手架 · Vue3 前端 · IPFS（可选）
      ↓
用户层    ：学生/作者 · 指导教师 · 院系教务 · 竞赛组委会
      ↓
网络/共识层：联盟链（教学可信节点）· 智能合约实现
      ↓
应用/服务层：
   ├─ 成果登记   ├─ 指纹计算   ├─ AIGC 声明
   ├─ 相似度核验 ├─ 证书导出   └─ 争议仲裁
```

#### 功能模块（5个，控制在demo可做范围）
1. **用户与身份模块**：MetaMask 登录 / 钱包地址绑定学号（接口：`connectWallet()`、`bindStudent(address, studentId)`）
2. **成果登记模块**：上传文档或粘贴代码 → 本地算 SHA-256 指纹 → 上链（接口：`registerWork(workHash, meta)`、`getWorkDetails(workId)`）
3. **AIGC 声明模块**：作者声明 AI 工具、用途、介入比例 → 上链存证（接口：`declareAIGC(workId, ratio, tools)`）
4. **核验模块**：任何人上传文件 → 比对链上指纹 → 返回"存在性 + 登记时间 + AIGC声明"（接口：`verifyWork(workHash)`）
5. **凭证与仲裁模块**：合约自动签发存证证书 / 相似度超阈值触发仲裁（接口：`issueCertificate(workId)`、`raiseDispute(workId, evidenceHash)`）

#### 关键智能合约（Solidity，AI 可生成，约 150-250 行）
```solidity
contract AcademicProof {
    struct Work { bytes32 contentHash; address author; uint256 timestamp; uint8 aigcRatio; string metaURI; bool disputed; }
    mapping(uint256 => Work) public works;
    event WorkRegistered(uint256 indexed workId, bytes32 contentHash, address author, uint256 timestamp);
    function registerWork(bytes32 _hash, uint8 _aigcRatio, string calldata _metaURI) external returns (uint256);
    function verifyWork(bytes32 _hash) external view returns (uint256 workId, address author, uint256 timestamp);
    function declareAIGC(uint256 _workId, uint8 _ratio, string calldata _tools) external;
    function raiseDispute(uint256 _workId, bytes32 _evidenceHash) external;
}
```

#### 最小可运行 Demo（5天可完成）
- **前端单页**：钱包连接 → 上传文件 → 显示指纹与上链时间 → 核验页输入文件返回结果
- **合约**：部署到 Sepolia 测试网（免费）
- **界面**：留痕时间轴 + 相似度比对结果卡 + 一键导出存证证书 PDF
- **录屏**：3分钟，走"上传 → 上链 → 换个人来核验 → 篡改后核验失败"的完整闭环

#### 演示脚本（天然有戏剧性）
> 上传论文 → 链上生成存证 → 手动修改文件一个字 → 再次核验 → **显示"核验不通过"**

这个"篡改即失效"的演示效果，是所有区块链作品里最有说服力的。

#### 风险与规避
- ⚠️ **不要真做全文相似度算法**（那是另一个课题）→ 用现成工具（如 `JPlag`、`simhash` 库）算一个相似度，只把**结果哈希**上链
- ⚠️ 大文件不要上链 → 只上**指纹**，原文放本地/IPFS

#### 双赛道适配度
- **行业方案赛道**：✅ 完美。提案书 = 痛点 + 架构 + 合约设计 + 界面原型
- **技术应用赛道**：✅ 完美。demo 就是上面那套，界面清晰、交互简单、演示效果强

---

### 🥈 选题2：算法合规链 —— 基于区块链的AI决策审查与责任追溯平台

> ⚠️ 注意：**这是 PPT 里给出的范例之一**（"算法合规链——AI司法审查与责任追溯平台"）。优点是踩中官方口味，缺点是**容易被认为在模仿范例**。要做就要做出明显差异。

**一句话定位**：让每一次 AI 决策都留下可审计、可归责、不可篡改的"决策血缘"。

#### 痛点分析
1. **AI 决策是黑箱**：信贷拒贷、简历筛选、内容审核……被拒绝的人拿不到理由，也找不到申诉对象。
2. **责任链断裂**：一个 AI 系统由数据提供方、模型训练方、部署运营方、人工复核方共同构成，出问题时**无法定位是哪个环节的错**。
3. **监管新要求**：算法备案、可解释性要求逐步落地，但**缺乏技术手段留痕**。
4. **模型版本漂移**：线上模型被静默更新，历史决策无法复现。

#### 为什么必须用区块链
- **决策记录上链** → 不可事后修改、不可选择性删除
- **决策血缘（数据→模型版本→推理→人工复核）链式存证** → 形成可追溯的因果链
- **智能合约自动执行申诉流程** → 申诉、举证、裁决全流程留痕

#### 架构要点
```
应用/服务层：算法注册 · 决策记录 · 责任链追踪 · 申诉仲裁 · 合规报告
网络/共识层：联盟链（监管节点 + 企业节点 + 第三方审计节点）· 智能合约
用户层    ：算法提供方 · 企业运营方 · 监管机构 · 被决策个体
系统架构层：FISCO-BCOS · 链下可解释性服务（SHAP/LIME）· 图数据库 · Vue前端
```

#### 关键接口设计
```solidity
contract AlgorithmCompliance {
    function registerAlgorithm(string name, string version, bytes32 modelHash) external returns (uint256);
    function logDecision(uint256 algoId, bytes32 inputHash, bytes32 outputHash, string reasonURI) external returns (uint256);
    function traceResponsibility(uint256 decisionId) external view returns (address[] memory chain);
    function fileAppeal(uint256 decisionId, string calldata reason) external;
    function resolveAppeal(uint256 appealId, bool upheld) external;
}
```

#### 最小 Demo
- 模拟一个"简历筛选 AI"（用规则引擎或调用开源小模型即可，**不需要真训模型**）
- 每条决策生成记录 → 上链 → 页面可查"决策血缘图"
- 一个"申诉"按钮 → 走完整仲裁流程

#### 风险与规避
- ⚠️ **最大风险是"解释性做空"** → 不要承诺真做可解释AI，把重点放在**"留痕与归责的机制设计"**上，解释性部分用现成库（SHAP）演示即可
- ⚠️ 与 PPT 范例撞题 → **差异化**：聚焦"多主体责任链拆分"而不是"司法场景"

---

### 🥉 选题3：链享凭证 —— 基于区块链的电商退货/二手转卖可信流转平台

> 结构参考 PPT 里的"**鲜享链 DAPP：基于区块链的生鲜 NFT 礼券交易平台**"（铸造→发行→购买→交易→兑换→销毁）

**一句话定位**：让"这件商品被退过几次、经手过谁"无法被抹掉。

#### 痛点分析
1. **退货商品洗白**：退货商品重新包装当新品卖，消费者无从知晓。
2. **二手交易信任缺失**：闲鱼类平台靠信用分和评价，**可刷、可删、可造假**。
3. **正品验证困难**：品牌防伪码可被批量复制，验证结果由品牌单方控制。
4. **售后纠纷举证难**：谁在什么时候拆封、维修、转卖，缺乏可信记录。

#### 为什么必须用区块链
- **流转记录上链不可删改** → 商品"履历"永久可查
- **NFT 状态机** → 全新 → 退货 → 检测 → 二次销售 → 售后，状态转换由合约强制执行
- **多方共同写入** → 品牌、物流、检测方、平台各写各的，单方无法篡改

#### 关键设计
```solidity
contract ItemPassport {
    enum State { Minted, Returned, Inspected, Resold, Redeemed, Burned }
    struct Passport { bytes32 itemHash; State state; address currentHolder; uint256[] history; }
    function mintPassport(bytes32 itemHash, address holder) external returns (uint256);
    function transferPassport(uint256 id, address to) external;
    function markReturned(uint256 id, string calldata reason) external;
    function inspect(uint256 id, bool passed, uint8 grade) external;
    function redeem(uint256 id) external;
    function burnExpired(uint256 id) external;
}
```

#### 最小 Demo
- 商家铸造 → 消费者购买 → 发起退货 → 检测方定级 → 二次销售（页面明示"退货再售，成色B"）→ 过期销毁
- 前端：**商品生命周期时间轴图**（这个可视化最容易做得好看）+ 扫码验真页面

#### 风险与规避
- ⚠️ **同质化严重**（溯源类作品太多）→ 差异化点必须打"**退货再售透明化 + 成色定级**"这个具体场景，而不是泛泛的"商品溯源"
- ⚠️ 需要虚拟数据 → 提前造 5-10 条完整的商品生命周期的假数据（合理合法，标注为演示数据）

---

### 🏅 选题4：校园资源链 —— 基于区块链的实验室/设备共享预约与信用治理平台

**一句话定位**：让"谁预约了、谁损坏了、谁守信"在学院内公开可查、自动约束。

#### 痛点分析（真实到可以直接找学院要应用证明）
1. **预约靠群接龙 / Excel**：被抢占、忘记、重复预约，无人负责。
2. **违约零成本**：约了不来、超时占用，没有任何后果。
3. **设备损坏扯皮**：谁最后使用、损坏是否及时上报，全靠口头。
4. **共享设备利用率低**：热门设备排队，冷门设备闲置，缺乏透明调度依据。

#### 为什么必须用区块链
- **预约记录上链** → 不可事后涂改，"我明明约了"这类争议消失
- **信用分由合约自动计算** → 守约加分、违约扣分，规则公开透明，**不由管理员说了算**
- **设备使用与责任绑定** → 谁在什么时段使用、有无异常上报，全程留痕

#### 系统架构
```
应用/服务层：预约管理 · 信用评分 · 设备台账 · 违约仲裁 · 数据看板
网络/共识层：联盟链（学院节点 + 实验室节点 + 学生会节点）· 智能合约
用户层    ：学生 · 实验室管理员 · 学院 · 导师
系统架构层：FISCO-BCOS · 预言机（门禁/IoT数据源）· Vue3 + ECharts 看板
```

#### 关键设计
```solidity
contract CampusResource {
    struct Booking { uint256 resourceId; address user; uint256 start; uint256 end; bool checkedIn; bool violated; }
    mapping(address => int256) public creditScore;
    function book(uint256 resourceId, uint256 start, uint256 end) external returns (uint256);
    function checkIn(uint256 bookingId) external;              // 可由预言机自动调用
    function reportViolation(uint256 bookingId, string calldata evidence) external;
    function slashCredit(address user, int256 delta) external;  // 仅合约规则可调用
    function getCredit(address user) external view returns (int256);
}
```

#### 最小 Demo
- 预约页（选时段 → 上链 → 生成预约凭证）
- 签到按钮（模拟门禁打卡）
- 信用分看板（ECharts 热力图 / 排行榜）
- **演示亮点**：故意违约 → 信用分自动下降 → 下次预约被拒

#### 差异化与技术亮点
- **链上信用模型**（可写成"基于智能合约的多方共治信用评分机制"）
- **预言机接入**（用树莓派或一个 HTTP 服务模拟门禁数据，PPT 里也提到了预言机）
- **最能拿到"应用证明"**：真的在学院/实验室试运行一周，让管理员出一份应用证明 → **"价值前景"和"成果展示"直接拿高分**（PPT 明确说"第三方测试报告或应用证明"是加分项）

#### 风险与规避
- ⚠️ 别真做硬件 → 用 HTTP 接口模拟门禁，或用树莓派做**可选加分项**
- ⚠️ 隐私问题 → 只存"预约记录 + 信用分"，不存个人信息；学号用哈希

---

### 🏅 选题5：履链 —— 基于区块链的可验证履历与选择性披露平台

**一句话定位**：让竞赛获奖、实习经历、课程学分变成企业一扫即验、且你能控制披露范围的链上凭证。

#### 痛点分析
1. **证书造假成本低**：简历上的获奖、实习证明 P 图太容易，企业核验成本极高。
2. **核验流程繁琐**：企业要打电话给学校、发邮件给主办方，周期长。
3. **隐私两难**：要证明"我拿过国奖"，就得把整张证书给对方看，暴露姓名、学号、身份证后四位。
4. **跨机构不互认**：课程学分、竞赛成绩、培训证书分散在各系统，无法整合。

#### 为什么必须用区块链
- **凭证哈希上链 + 颁发方签名** → 企业扫码即可验证真伪，**不需要联系学校**
- **选择性披露** → 用默克尔证明/零知识证明，只证明"分数 ≥ 90"而不暴露具体分数
- **颁发方可撤销** → 造假被发现后，链上凭证可被标记失效

#### 关键设计
```solidity
contract VerifiableCredential {
    struct Credential { bytes32 claimHash; address issuer; address holder; uint256 issuedAt; bool revoked; }
    mapping(uint256 => Credential) public credentials;
    function issue(address holder, bytes32 claimHash, string calldata credType) external returns (uint256);
    function revoke(uint256 credId) external;                              // 仅 issuer
    function verify(uint256 credId) external view returns (bool valid, address issuer);
    function verifyRange(uint256 credId, bytes32 proof, uint256 minValue) external view returns (bool); // 选择性披露
}
```

#### 最小 Demo
- **颁发端**（模拟学校/主办方）：录入一条获奖记录 → 签发凭证 → 生成二维码
- **持有端**（学生）：履历面板，选择"向这家企业披露哪些项"
- **验证端**（企业）：扫码 → 显示"✅ 凭证有效，颁发方：东华大学计算机学院，颁发时间：…"

#### 风险与规避
- ⚠️ **零知识证明是最大的坑** → 15天内**不要真实现 zk-SNARK**。用"哈希承诺 + 范围证明的简化版（如哈希+盐值验证）"即可，文档里写清"ZK 为设计目标，当前实现为简化方案"，**诚实比吹牛安全**（一票否决里有"关键信息缺失/造假"）
- ⚠️ 需要"颁发方"角色 → 演示时你自己扮演，并说明"已与学院沟通试点"

---

## 三、我的推荐排序

| 排名 | 选题 | 理由 |
| --- | --- | --- |
| **1** | **选题1 科研链证** | 痛点真实且你是用户；demo"篡改即失效"演示效果最强；工程量最可控；与课程/竞赛场景天然结合，好写"应用价值" |
| **2** | **选题4 校园资源链** | 最容易拿到**真实应用证明**（直接换"成果展示"和"价值前景"分）；有链上信用模型这个技术亮点；学院场景有代入感 |
| **3** | 选题3 链享凭证 | 最容易做出好看的可视化；但溯源类同质化严重，需要靠"退货再售"这个切口破局 |
| 4 | 选题5 履链 | 隐私/选择性披露是高级技术点，但易过度承诺，答辩风险高 |
| 5 | 选题2 算法合规链 | 踩官方口味，但**PPT 里已有范例**，容易被认为跟风；且解释性部分容易做空 |

### 🎯 最终建议

**主选：选题1（科研链证）**
**如果学院愿意配合试点：改选选题4（校园资源链）** —— 因为它多一张"应用证明"，性价比最高。

**选题技巧**：无论选哪个，标题都套用这个模板：
> **"[两个字的具象名] + 链" + "——基于区块链的 XX 存证/治理平台"**

例如：`研链证`、`资链通`、`享链证`、`履链通`

---

## 四、22天执行节奏（以选题1为例）

| 阶段 | 时间 | 产出 | AI 能帮多少 |
| --- | --- | --- | --- |
| **选题定稿** | 9/18-19 | 选题定位文档（行业场景/用户/痛点/定位） | 帮你列痛点、查同类竞品 |
| **架构设计** | 9/20-22 | 四层架构图、功能模块图、业务流程图、时序图 | **几乎全包**（Mermaid/draw.io 代码） |
| **合约开发** | 9/23-25 | Solidity 合约 + 部署到 Sepolia + 测试 | **几乎全包**，你负责跑通 |
| **前端开发** | 9/26-30 | Vue3 单页：连接钱包/上传/上链/核验/时间轴 | **大部分**，你负责调 UI |
| **文档撰写** | 10/1-4 | 创意提案书（概要设计+详细设计+测试分析） | **大部分**，你负责技术准确性 |
| **演示材料** | 10/5-7 | 3分钟演示视频 + PPT + 源代码仓库整理 | 帮你写脚本、做 PPT 大纲 |
| **提交** | 10/8 | 双赛道提交（行业方案 + 技术应用） | — |

⚠️ **10月8日提交，不要等10月9日。**

---

## 五、答辩预判：评委一定会问的5个问题

提前把答案写进文档，答辩就不会慌：

1. **"这个功能用普通数据库不也能做吗？为什么非要区块链？"**
   → 答：核心是**多方互不信任、且需要第三方独立验证**。中心化数据库里，记录方 = 验证方，自证无效。

2. **"你的链下数据怎么保证上链前没被改过？"**
   → 答：设计"**提交即上链**"流程 + 多签见证 + 后续版本追加，承认**首次上链前的真实性依赖源头可信**（这是所有上链方案的共同边界，坦白回答反而加分）。

3. **"性能能撑住吗？"**
   → 答：明确说明**只有哈希和关键状态上链，原文和计算放链下**，这是标准的分层设计。

4. **"你一个人做的？工作量匹配吗？"**
   → 答：**主动说明独立完成了哪些部分**（架构设计、合约、前端、文档），并展示 commit 记录。

5. **"和已有的 XX 产品有什么区别？"**
   → 答：准备一个"竞品对比表"放在文档里，**这是 PPT 里"选题定位"得分的隐藏加分项**。

---

## 附：从两份 PPT 里提取的评分与要求速查

### 行业方案赛道（PPT 原文要点）
- 选题：区块链+金融/农业/法律/管理/教育等；须明确"位于行业应用的哪个节点、用户对象"
- 设计完成与呈现 50分：创新能力（新领域/新方法/新途径）+ 技术能力（功能需求分析、功能模块设计、系统框架图、界面及交互性、测试与分析）+ 区块链技术运用（智能合约、加密算法、数字签名、共识机制）
- 测试建议包含：功能测试、智能合约测试、性能测试、安全测试、兼容性测试、可用性测试、压力测试（按需体现）
- 技术规范：1.作品文档文件夹 2.源代码文件夹（**证明作品完成时间、过程**）3.说明文档文件夹 4.演示汇报文件夹
- **"不硬性要求解决方案的原代码、Web端或移动端平台"**

### 技术应用赛道（PPT 原文要点）
- 选题分类：应用设计 / 技术创新 / 安全机制 / 融合技术
- 优秀作品6个必要条件：必须区块链要素 · 有创意 · 系统设计和代码解释 · 设计图 · 优秀图形界面 · 优秀视频讲解
- 展示要求：DEMO或视频，作品稳定无运行错误；创新技术作品需附**技术指标实验视频和结果**

### 一票否决红线
1. 选题存在意识形态问题
2. 抄袭、侵权、盗用数据与代码，使用非授权素材
3. **作品非本届大赛规定周期内原创完成**
4. **文档、演示、系统存在造假或关键信息缺失**
5. 含其他赛事标识、违规水印、商业广告
